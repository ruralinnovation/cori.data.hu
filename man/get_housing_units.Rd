% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_hu.R
\name{get_housing_units}
\alias{get_housing_units}
\title{Get Census Bureau housing unit estimates}
\usage{
get_housing_units(
  geography = c("all", "county", "state", "nation"),
  years = NULL,
  geoids = NULL,
  variables = NULL,
  vintage = "latest"
)
}
\arguments{
\item{geography}{Character. Geographic level to return: \code{"all"}, \code{"county"},
\code{"state"}, or \code{"nation"}. Ignored when \code{geoids} is provided. Default: \code{"all"}.}

\item{years}{Integer vector. Years to return. Default: all available.}

\item{geoids}{Character vector. FIPS codes to return (5-digit county,
2-digit state, or \code{"00"} for national). When provided, takes precedence
over \code{geography}. Default: \code{NULL} (all geographies).}

\item{variables}{Character vector. Variables to return: \code{"housing_units"},
\code{"housing_units_per_1k_people"}, and/or \code{"housing_unit_change"}.
Default: all. See \code{\link[=get_hu_codebook]{get_hu_codebook()}} for details.}

\item{vintage}{Character. Vintage to read, e.g. \code{"2024"}. Default: \code{"latest"}.}
}
\value{
A data frame with columns: \code{geoid}, \code{year}, \code{variable}, \code{value},
\code{agg_var}. \code{agg_var} is population / 1,000 for \code{housing_units_per_1k_people}
(suitable for population-weighted averaging) and \code{NA} for count variables.
}
\description{
Returns housing unit estimates from the U.S. Census Bureau's National,
State, and County Housing Unit Totals. Data cover 2000–present at annual
frequency for county, state, and national geographies.
}
\examples{
\dontrun{
# All county-level housing unit estimates
get_housing_units(geography = "county", years = 2010:2024)

# Specific county — Grafton County, NH
get_housing_units(geoids = "33009")

# Housing units per 1,000 population, state level
get_housing_units(geography = "state", variables = "housing_units_per_1k_people")
}

}
\seealso{
\code{\link[=get_hu_codebook]{get_hu_codebook()}}
}
